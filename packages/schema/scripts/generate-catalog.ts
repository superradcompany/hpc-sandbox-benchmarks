#!/usr/bin/env bun
// `generate-catalog` — regenerate `src/pts-generated.ts` from catalog-eligible vendored profiles under
// `src/pts-profiles/`. Reads the committed XML (never the network), maps each profile to draft
// `MetricDef[]` (./catalog/generate.ts), serializes deterministically (./catalog/serialize.ts), and
// pipes the result through Biome so the committed bytes match a fresh run — the invariant the catalog
// drift gate enforces. Run by hand after vendoring/bumping a profile (`bun run generate-catalog`);
// nothing imports it at build time.
import { readdir } from "node:fs/promises";
// NOTE: importing catalog.ts also loads (and asserts) the COMMITTED catalog singleton — fine in the
// normal regen flow where the committed state is valid, and a hard failure anyway everywhere else.
import { catalogSchema } from "../src/catalog.ts";
import { generateCatalog } from "./catalog/generate.ts";
import { parseProfile } from "./catalog/parse.ts";
import { serializeCatalog } from "./catalog/serialize.ts";

const PROFILES_DIR = `${import.meta.dir}/../src/pts-profiles`;
const OUT_FILE = `${import.meta.dir}/../src/pts-generated.ts`;
const CATALOG_IGNORE_FILE = ".catalog-ignore";

async function readXml(path: string): Promise<string> {
	const file = Bun.file(path);
	return (await file.exists()) ? file.text() : "";
}

/**
 * Discover each catalog-eligible vendored profile as a `{ repo, dir }` pair. A profile-local
 * `.catalog-ignore` keeps specialized suites (for example GPU-only matrices) vendored and runnable
 * without pretending their heterogeneous metrics belong in the CPU sandbox comparison catalog.
 * A flat `pts-profiles/<name>-<ver>` (with a
 * test-definition.xml directly inside) is an upstream profile → repo `pts`. A `pts-profiles/<repo>/…`
 * directory (no test-definition.xml of its own, only child profile dirs) is a repo-local source → that
 * `<repo>` is the prefix (PTS reports e.g. `local/hardlink-1.0.0`). Sorted by repo then dir so the
 * generated output is independent of readdir order (the drift gate needs byte-stability).
 */
async function discoverProfiles(): Promise<Array<{ repo: string; dir: string; base: string }>> {
	const out: Array<{ repo: string; dir: string; base: string }> = [];
	for (const entry of await readdir(PROFILES_DIR, { withFileTypes: true })) {
		if (!entry.isDirectory()) continue;
		const path = `${PROFILES_DIR}/${entry.name}`;
		if (await Bun.file(`${path}/test-definition.xml`).exists()) {
			if (!(await Bun.file(`${path}/${CATALOG_IGNORE_FILE}`).exists())) {
				out.push({ repo: "pts", dir: entry.name, base: path });
			}
			continue;
		}
		// Repo subdir (e.g. `local/`): one level of nested profile dirs.
		for (const child of await readdir(path, { withFileTypes: true })) {
			if (!child.isDirectory()) continue;
			const childPath = `${path}/${child.name}`;
			if (
				(await Bun.file(`${childPath}/test-definition.xml`).exists()) &&
				!(await Bun.file(`${childPath}/${CATALOG_IGNORE_FILE}`).exists())
			) {
				out.push({ repo: entry.name, dir: child.name, base: childPath });
			}
		}
	}
	// Code-point order, not locale collation: the drift gate byte-compares the generated file, and
	// dir names carry hyphens/dots (e.g. `hardlink-1.0.0`) that locale rules can reorder per-machine.
	return out.sort((a, b) => {
		const ka = `${a.repo}/${a.dir}`;
		const kb = `${b.repo}/${b.dir}`;
		return ka < kb ? -1 : ka > kb ? 1 : 0;
	});
}

/** Regenerate `src/pts-generated.ts` in place. Exported so the drift gate can re-run it directly. */
export async function generateCatalogFile(): Promise<void> {
	const discovered = await discoverProfiles();

	const profiles = await Promise.all(
		discovered.map(async ({ repo, dir, base }) =>
			parseProfile(
				repo,
				dir,
				await readXml(`${base}/test-definition.xml`),
				await readXml(`${base}/results-definition.xml`),
			),
		),
	);

	const catalog = generateCatalog(profiles);

	// Validate the assembled catalog against the SAME load-time contract the runtime enforces
	// (catalogSchema's PTS-mapping invariants) BEFORE writing. The generator's own guards are
	// per-profile, so cross-profile shapes — e.g. two vendored versions of one test mixing wildcard
	// and described entries under the same versionless join key — would otherwise generate green and
	// then brick every subsequent `import @sandbox-benchmarks/schema` at module load. Failing here
	// names the problem at generation time instead. (Curation overrides only touch label/headline/
	// dimension, never `pts` or `unit`+`scale` together as a pair the generator didn't emit, so
	// validating the raw generated defs covers the merged catalog too.)
	catalogSchema.assert(catalog);

	await Bun.write(OUT_FILE, serializeCatalog(catalog));

	// Format in place with the repo's pinned Biome so re-generated bytes are byte-stable (drift gate).
	const fmt = Bun.spawn(["bunx", "biome", "format", "--write", OUT_FILE], {
		stdout: "inherit",
		stderr: "inherit",
	});
	if ((await fmt.exited) !== 0) throw new Error("biome format failed on generated catalog");

	console.log(
		`✓ ${catalog.length} metrics from ${profiles.length} profiles -> src/pts-generated.ts`,
	);
}

if (import.meta.main) {
	try {
		await generateCatalogFile();
	} catch (err) {
		// Log the whole error (preserves the stack) so a failure is debuggable, not just its message.
		console.error("generate-catalog failed:", err);
		process.exit(1);
	}
}
