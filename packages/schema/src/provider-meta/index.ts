// GENERATED by scripts/generate-provider-registry.ts. Edit provider files or PROVIDER_IDS, then regenerate.
import type { ProviderId } from "../provider-ids.ts";
import type { ProviderArtifact, ProviderMetaModule, ProviderMetaSource } from "../provider-meta.ts";
import blaxel from "./blaxel.ts";
import daytona_container from "./daytona-container.ts";
import daytona_vm from "./daytona-vm.ts";
import e2b from "./e2b.ts";
import microsandbox_cloud from "./microsandbox-cloud.ts";
import modal_gvisor from "./modal-gvisor.ts";
import modal_vm from "./modal-vm.ts";
import namespace from "./namespace.ts";
import novita from "./novita.ts";
import runcloud from "./runcloud.ts";
import runloop from "./runloop.ts";
import tama from "./tama.ts";
import vercel from "./vercel.ts";

const MODULES = {
	e2b: e2b,
	"daytona-vm": daytona_vm,
	"daytona-container": daytona_container,
	blaxel: blaxel,
	"microsandbox-cloud": microsandbox_cloud,
	"modal-gvisor": modal_gvisor,
	"modal-vm": modal_vm,
	novita: novita,
	runloop: runloop,
	namespace: namespace,
	vercel: vercel,
	runcloud: runcloud,
	tama: tama,
} as const satisfies { [P in ProviderId]: ProviderMetaModule<P> };

export const REGISTRY = {
	e2b: MODULES.e2b.meta,
	"daytona-vm": MODULES["daytona-vm"].meta,
	"daytona-container": MODULES["daytona-container"].meta,
	blaxel: MODULES.blaxel.meta,
	"microsandbox-cloud": MODULES["microsandbox-cloud"].meta,
	"modal-gvisor": MODULES["modal-gvisor"].meta,
	"modal-vm": MODULES["modal-vm"].meta,
	novita: MODULES.novita.meta,
	runloop: MODULES.runloop.meta,
	namespace: MODULES.namespace.meta,
	vercel: MODULES.vercel.meta,
	runcloud: MODULES.runcloud.meta,
	tama: MODULES.tama.meta,
} as const satisfies Record<ProviderId, ProviderMetaSource>;

type IdsWithArtifact<K extends ProviderArtifact["kind"]> = {
	[P in ProviderId]: (typeof REGISTRY)[P]["artifact"]["kind"] extends K ? P : never;
}[ProviderId];

export type BakedProviderId = IdsWithArtifact<"baked">;
export type ImageProviderId = IdsWithArtifact<"image">;
export type MirroredProviderId = IdsWithArtifact<"mirror">;
export type BuiltProviderId = IdsWithArtifact<"built">;
export type StockProviderId = IdsWithArtifact<"none">;
